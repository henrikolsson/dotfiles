[user]
	name = Henrik Olsson
	email = henrik@fixme.se
[alias]
	incoming = "!git remote update -p; git log ..@{u}"
	outgoing = log @{u}..
[push]
	default = simple
[color]
	ui = auto

[alias]
        fl = log --graph --stat --decorate --all
        st = status
        ci = commit
